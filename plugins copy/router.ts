export default defineNuxtPlugin((nuxtApp) => {
  // const store = useStore()
  // nuxtApp.hooks.beforeEach((to, from, next) => {
  //   store.commit('setMask',true)
  //   next();
  // })
  // nuxtApp.hooks.afterEach((to, from) => {
  //   setTimeout(() => {
  //     store.commit('setMask', false)
  //   }, 550)
  // })
});
